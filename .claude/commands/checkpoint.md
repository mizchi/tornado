Save a checkpoint of the current work state:

1. Run `just test` to verify all tests pass
2. Run `git status` to show current changes
3. Run `git stash create` to create a checkpoint ref
4. Report the checkpoint hash and what it contains
